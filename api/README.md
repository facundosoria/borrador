# API Module (Spring Boot 3.x)

Monolito modular responsable de reglas, persistencia y tiempo real.

Dominios iniciales:
- `auth`
- `users`
- `cards`
- `deck`
- `lobby`
- `game` (`engine`, `realtime`, `persistence`)
