# C4 Context (Draft)

- User interacts with `front` (Angular).
- `front` communicates with `api` using REST and WebSockets.
- `api` persists and reads data from PostgreSQL.
- Optional external card API integration is limited to ingestion/update jobs.
